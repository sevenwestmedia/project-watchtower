{
    "include": ["src", "@types"],
    "exclude": ["dist"],
    "extends": "../../tsconfig.cjs.json",
    "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "baseUrl": ".",
        "paths": {
            "@project-watchtower/server": ["../server"]
        }
    },
    "references": [
        {
            "path": "../server"
        }
    ]
}
